/**
 * This file is generated by the openapi-ts-generator
 * #form-group-patcher-ts.hbs
 * For issues or feature request, visit the repo: https://github.com/ikemtz/openapi-ts-generator
 * Do not edit.
 */
/* tslint:disable */

import { FormGroup } from '@angular/forms';
import { tap, filter } from 'rxjs/operators';
import { Observable } from 'rxjs';
import { I{{name}}, {{name}}Properties } from './{{kebabName}}.model';

export const formPatcher = (addEditForm: FormGroup) => (source: Observable<I{{name}}>) => source.pipe(
  filter(t => !!t),
  tap(t => {
    addEditForm.patchValue({
{{#each propertyKeys}}
      [{{@root.name}}Properties.{{this.nameSnakeCase}}]: t.{{this.name}},
{{/each}}
    });
  })
);